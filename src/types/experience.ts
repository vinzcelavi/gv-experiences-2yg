export interface Experience {
  id: string;
  title: string;
  image: string;
  rating: number;
  reviewCount: number;
  provider: string;
  startingPrice: number;
  description: string;
  location: string;
}